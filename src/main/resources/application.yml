server:
  port: 8788

spring:
  datasource:
    url: jdbc:mysql://localhost:17777/chatting?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true
    username: chat-user
    password: my-secret-pw
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
#logging:
#  level:
#    org.hibernate.SQL: DEBUG # SQL 문을 로그로 출력
#    org.hibernate.type: TRACE # 바인딩된 파라미터 값을 로그로 출력